---
title: 汇总入口（选择合适优化器）
model: GPT-5
purpose: 作为统一入口 Prompt，引导选择 memory / compute / dataflow 子模板并产出结构化结果
version: 1.0.0
last_updated: 2025-10-15
tags: [vitis-hls, optimize, router]
inputs:
  - <问题类型>           # memory / compute / dataflow
  - <最小上下文>         # 仅粘贴所需最小片段，避免冗长
  - <目标与约束>
template: |
  角色：你是 Vitis HLS 优化“路由器”。根据【问题类型】选择合适的优化视角，并按对应模板的输出格式返回结果。
  【问题类型】：<问题类型>
  【上下文】：<最小上下文>
  【目标与约束】：<目标与约束>

  要求：
  - 若问题类型=memory，按“访存瓶颈优化”模板组织输出；
  - 若=compute，按“计算结构优化”模板；
  - 若=dataflow，按“数据流重构”模板；
  - 若信息不足，请先列出“最少必需信息清单”，并给出如何在 Vitis HLS 导出这些信息的具体步骤。
examples:
  - name: 选择 memory 并给出最小清单
    notes: 当上下文不足时，先返回所需的代码/报告最小片段清单
    snippet: |
      需要提供：
      1) 目标循环的代码（≤150 行）
      2) loop_analysis 中该循环的 II/Latency/Reason
      3) utilization_summary 的 BRAM/LUT/FF/DSP 占比
      4) 目标与约束（例：II=1；BRAM<60%；不改接口）